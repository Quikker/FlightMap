#include "Flight.h"

Flight::Flight(const City baseCity, const City nextCity, const int flightID, const int flightCost):
				 baseCity(baseCity),  nextCity(nextCity), flightID(flightID), flightCost(flightCost)
{
}
